{
  "name": "cristianvega-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run build && npm run check && npm test",
    "generate:halftone": "node scripts/generate-halftone.mjs",
    "generate:portrait": "node scripts/generate-portrait.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.1",
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "sharp": "^0.35.3",
    "typescript": "^5.9.3"
  }
}
